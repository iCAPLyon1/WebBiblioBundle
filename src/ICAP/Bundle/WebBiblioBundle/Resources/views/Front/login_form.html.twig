<div class="connectBlock">
    {% if error %}
        <div>{{ error.message }}</div>
    {% endif %}

    <div class="connectHeader">Please enter your email and password</div>
    <form action="{{ path('web_biblio_connect') }}" method="post">
        <label for="username">Email:</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}" />

        <label for="password">Password:</label>
        <input type="password" id="password" name="_password" />

        {#
            If you want to control the URL the user is redirected to on success (more details below)
            <input type="hidden" name="_target_path" value="/account" />
        #}
        <input type="hidden" name="_target_path" value="/web-biblio" />

        <button type="submit">login</button>
    </form>


    {#
    <div class="connectHeader">Please enter your username</div>
    <form method="POST" action="{{ path('web_biblio_connect') }}">
        {{ form_widget(form) }}
        <input type="submit" value="Submit" />
    </form>
    #}
</div>

